package com.getpcpanel.device.descriptor;

/**
 * Which logical group a light output belongs to, mirroring the PCPanel lighting buckets.
 */
public enum LightGroupKind {
    DIAL, SLIDER, SLIDER_LABEL, LOGO, GENERIC
}

package com.getpcpanel.util;

/**
 * Image resources stub — JavaFX removed as part of Quarkus migration.
 * Icon loading now handled via REST API endpoints returning base64-encoded images.
 */
public abstract class Images {
    private Images() {
    }
}

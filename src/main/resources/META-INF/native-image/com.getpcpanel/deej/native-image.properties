# GraalVM native-image auto-discovers this directory and merges the *-config.json files here
# (jni-config.json + reflect-config.json) registering com.fazecast.jSerialComm.SerialPort so the
# bundled JNI native library can reflect into its fields/ctor at runtime (see jSerialComm #438).
